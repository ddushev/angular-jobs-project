import { JobInputValidationDirective } from './job-input-validation.directive';

describe('JobInputValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new JobInputValidationDirective();
    expect(directive).toBeTruthy();
  });
});
